version: '3.8'
services:
  dev-db:
    image: postgres:13
    container_name: barter_db
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres